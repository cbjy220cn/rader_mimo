# 断点续跑功能说明

## 🎯 功能特性

`comprehensive_validation.m` 现在支持**断点续跑**，即使中途中断（Ctrl+C）或程序崩溃，也能从上次停止的地方继续。

---

## 📋 工作原理

### 三级保存机制

1. **实验级保存**：每完成一个实验（实验1、2、3）立即保存
2. **SNR点级保存**：实验3的每个SNR点完成后立即保存
3. **总进度保存**：记录当前完成到哪个实验

### 保存的文件

```
validation_results/
├── progress.mat                    # 总进度文件
├── exp1_resolution_results.mat     # 实验1结果
├── exp2_aperture_results.mat       # 实验2结果
├── exp3_rmse_progress.mat          # 实验3中间进度（每个SNR点）
├── exp3_rmse_results.mat           # 实验3最终结果
└── [所有图片文件]
```

---

## 🚀 使用方法

### 情况1：首次运行

```matlab
comprehensive_validation
```

脚本会从头开始运行所有实验。

---

### 情况2：中途中断后继续

**中断方式**：
- Ctrl+C 手动中断
- 程序崩溃
- 电脑关机/断电

**恢复运行**：
```matlab
comprehensive_validation
```

脚本会检测到进度文件，询问：
```
🔄 检测到之前的进度文件，是否继续？
   如果继续，将跳过已完成的实验
   如果重新开始，将删除之前的结果
   输入 1=继续, 0=重新开始 [1]:
```

- 输入 `1` 或直接回车：继续之前的进度
- 输入 `0`：清空进度，重新开始

---

### 情况3：实验3（最耗时）的断点续跑

实验3（RMSE vs SNR）是最耗时的部分，它会：

1. **每完成一个SNR点就保存**：
   ```
   [1/6] SNR = -5 dB ... RMSE: 静态=15.02°, 旋转=9.14° (耗时125.3秒)
        💾 进度已保存 (完成 1/6 个SNR点)
   ```

2. **中断后自动恢复**：
   ```
   🔄 检测到实验3的中间结果，从SNR点 2 继续
   
   [2/6] SNR =  0 dB ...
   ```

3. **显示已完成的部分**：
   - 跳过已计算的SNR点
   - 直接从下一个SNR点开始

---

## 🛠️ 辅助工具

### 1. 查看进度

```matlab
check_validation_progress
```

显示：
- 当前完成了哪些实验
- 实验3完成了多少个SNR点
- 已保存的文件列表
- 已生成的图像列表

输出示例：
```
📊 总体进度:
   最后完成的实验: 2
   ✅ 实验1: 角度分辨率测试 - 已完成
   ✅ 实验2: 有效孔径扩展 - 已完成
   ⏸️  实验3: RMSE vs SNR - 未开始或进行中

🔬 实验3详细进度:
   完成的SNR点: 3 / 6
   已完成的SNR点: [-5 +0 +5] dB
   ⏭️  下一个: SNR = +10 dB
```

---

### 2. 重置进度

```matlab
reset_validation_progress
```

提供三个选项：
1. **仅重置实验3**：保留实验1和2，只重跑实验3
2. **重置所有进度**：保留数据文件和图片，但从头运行
3. **删除所有文件**：完全清空，从零开始

---

## 📊 典型使用场景

### 场景A：远程服务器运行

```bash
# 在远程服务器启动
nohup matlab -batch "comprehensive_validation" > validation.log 2>&1 &

# 如果中断，再次运行会自动继续
nohup matlab -batch "comprehensive_validation" >> validation.log 2>&1 &
```

### 场景B：本地电脑分段运行

```matlab
% 第一天：运行到实验1完成就中断
comprehensive_validation
% [手动 Ctrl+C]

% 第二天：继续运行实验2
comprehensive_validation
% [自动跳过实验1，运行实验2]
% [手动 Ctrl+C]

% 第三天：完成实验3和绘图
comprehensive_validation
% [自动跳过实验1和2，运行实验3]
```

### 场景C：实验3部分失败

```matlab
% 运行到SNR=10dB时崩溃
comprehensive_validation
% [完成了SNR=-5, 0, 5dB，在10dB时崩溃]

% 重新运行
comprehensive_validation
% 输出: 🔄 检测到实验3的中间结果，从SNR点 4 继续
% [自动跳过-5, 0, 5dB，从10dB开始]
```

---

## ⚙️ 时间估算

基于典型配置（8元阵列，64快拍）：

| 实验 | 预计时间 | 可中断性 |
|-----|---------|---------|
| 实验1 | ~10-15分钟 | 整体保存 |
| 实验2 | ~5-10分钟 | 整体保存 |
| 实验3 | **~20-40分钟** | **每个SNR点保存** |
| 绘图 | ~1-2分钟 | 快速 |

**总计**：约30-70分钟

**实验3详细**（6个SNR点 × 20次试验）：
- 每个SNR点：3-7分钟
- 单次trial：10-20秒（取决于快拍数）

---

## 💡 最佳实践

### 1. 长时间运行的建议

```matlab
% 使用日志记录
diary('validation_run.log');
diary on;
comprehensive_validation
diary off;
```

### 2. 定期检查进度

在另一个MATLAB窗口或终端：
```matlab
check_validation_progress
```

### 3. 紧急恢复

如果进度文件损坏：
```matlab
% 手动设置进度
output_dir = 'validation_results';
progress.last_completed_experiment = 2;  % 假设完成到实验2
save(fullfile(output_dir, 'progress.mat'), 'progress');
```

---

## 🔧 技术细节

### 进度文件格式

**progress.mat**:
```matlab
progress = struct(
    'last_completed_experiment', 2,    % 0-4
    'completion_time', '16-Nov-2025 10:30:45'
);
```

**exp3_rmse_progress.mat**:
```matlab
exp3_progress = struct(
    'rmse_static', [15.02, 13.45, 12.10, 0, 0, 0],
    'rmse_rotating', [9.14, 2.78, 1.95, 0, 0, 0],
    'last_snr_idx', 3,
    'snr_range', [-5, 0, 5, 10, 15, 20]
);
```

### 数据完整性

- 所有保存使用MATLAB的`save`命令，原子性操作
- 即使保存时中断，不会损坏已有数据
- 最坏情况：丢失最后一个trial的结果

---

## ❓ 常见问题

### Q1: 如何确认数据已保存？

**A**: 查看命令行输出：
```
💾 实验1结果已保存
💾 进度已保存 (完成 3/6 个SNR点)
```

### Q2: 中断后数据丢失了多少？

**A**:
- 实验1/2：最多丢失当前实验的所有数据
- 实验3：最多丢失当前SNR点的20次trial
- 绘图：无数据丢失

### Q3: 可以手动修改进度吗？

**A**: 可以，但要小心：
```matlab
load('validation_results/progress.mat');
progress.last_completed_experiment = 1;  % 强制从实验2开始
save('validation_results/progress.mat', 'progress');
```

### Q4: 如何完全重新运行？

**A**: 删除进度文件：
```matlab
delete('validation_results/progress.mat');
delete('validation_results/exp3_rmse_progress.mat');
```

或使用：
```matlab
reset_validation_progress
% 选择选项2或3
```

---

## 📝 更新日志

- **v1.1** (当前): 添加断点续跑功能
  - 三级保存机制
  - 实验3 SNR点级保存
  - 辅助工具脚本

- **v1.0** (原始): 基础验证系统
  - 必须一次性运行完成

---

**总结**：现在可以放心在远程服务器或不稳定环境中运行，随时中断，随时恢复！

